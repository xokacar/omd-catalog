# Dockerfile for openmetadata-server service
FROM docker.getcollate.io/openmetadata/server:1.3.0

# Add custom configuration if needed
# No custom configuration provided in the original docker-compose.yml

# Expose ports 8585 and 8586
EXPOSE 8585
EXPOSE 8586

# Command to run the openmetadata-server container
CMD ["./startup_script.sh"] # Replace with the actual startup script if any
